import { StatusBar } from 'expo-status-bar';
import React from 'react';
import { useState } from 'react';
import { Text, View } from 'react-native';

const Shuffle = () => {
  return (
    <View>
      <Text>Shuffle through some Music!</Text>
    </View>
  )
};

export default Shuffle;